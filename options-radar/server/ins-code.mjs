// صحت‌سنجی کد ابزار.
//
// `ins` از پرس‌وجو مستقیم داخل مسیر بالادست می‌نشیند: ‎/BestLimits/${ins}‎.
// بدون سنجش، با `../` می‌شد مسیر را به نقطه پایانی دیگری برد. کد ابزار
// همیشه عدد است، پس هر چیز دیگری رد می‌شود.

export const isInsCode = (s) => /^\d+$/.test(s);
