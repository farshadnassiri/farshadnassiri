// صحت‌سنجی ورودی‌های عمومی سرور.

// کد ابزار تسمک همیشه رشته‌ای از رقم است. صحت‌سنجی این‌جا لازم است چون کد
// مستقیم داخل مسیر بالادست می‌نشیند — بی‌آن، با «..» می‌شود مسیر را به
// نقطه پایانی دیگری برد.
const INS_RE = /^\d+$/;
export const isValidIns = (x) => typeof x === 'string' && INS_RE.test(x);
