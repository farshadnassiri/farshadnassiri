// اعتبارسنجی ورودی‌های خام درخواست پیش از رفتن به مسیر بالادست.
//
// `ins` مستقیم داخل مسیر بالادست می‌نشیند (`/BestLimits/${ins}`)، پس باید
// پیش از استفاده شکلش تضمین شود. کد ابزار همیشه عدد است.

export const INS_RE = /^\d+$/;
export const isValidIns = (v) => INS_RE.test(v || '');
